<!-- Input.stories.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { COLORS } from '../../../constants'

import Dropdown from './dropdown';

<Meta title="Atoms/Dropdown" component={Dropdown} />

export const Template = (args) => {
  return (
    <div style={{
      padding: '30px',
      background: args.color === 'dark' ? COLORS.PRIMARY : COLORS.BLUE_WHITE
    }}>
      <Dropdown {...args} />
    </div>
  )
};

# Dropdown

##  Usage

This is `Dropdown` atom, used to insert an selectable list of items in the UI.

The component supports being used in a light or dark background, specifying the `color` prop:

- `light`
- `dark`

This is the default style, it is compact, use it when the component is being used along with other inputs in a long form

<Canvas withSource="open">
  <Story
    name="light"
    args={{
      color: 'light',
      items: [{
        label: "Item 1",
        value: 1
      }, {
        label: "Item 2",
        value: 2
      }, {
        label: "Item 3",
        value: 3
      }, {
        label: "Item 4",
        value: 4
      }]
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas withSource="open">
  <Story
    name="dark"
    args={{
      color: 'dark',
      items: [{
        id: "item_1",
        label: "Item 1",
        value: 1
      }, {
        id: "item_2",
        label: "Item 2",
        value: 2
      }, {
        id: "item_3",
        label: "Item 3",
        value: 3
      }, {
        id: "item_4",
        label: "Item 4",
        value: 4
      }]
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
