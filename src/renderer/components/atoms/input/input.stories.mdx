<!-- Input.stories.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { AlertIcon } from '@primer/octicons-react';
import { COLORS } from '../../../constants'

import Input from './input';

<Meta title="Atoms/Input" component={Input} />

export const Template = (args) => {
  return (
    <div style={{
      padding: '30px',
      background: args.color === 'dark' ? COLORS.PRIMARY : COLORS.BLUE_WHITE
    }}>
      <Input {...args} />
    </div>
  )
};

# Input Field

##  Usage

This is `Input` atom, used to insert an `<input>` field on the UI. It is a controlled component and it receives all the props that a normal input would receive, plus the following ones.
There are 2 styles for this component, indicated by the `as` prop:

- `one-liner`
- `rounded`

Also the component supports being used in a light or dark background, specifying the `color` prop:

- `light`
- `dark`

### One Liner Style

This is the default style, it is compact, use it when the component is being used along with other inputs in a long form

<Canvas>
  <Story
    name="one-liner-light"
    args={{
      as: 'one-liner',
      color: 'light',
      placeholder: 'Placeholder',
      disabled: false
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="one-liner-light-disabled"
    args={{
      as: 'one-liner',
      placeholder: 'Disabled',
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
    <Story
      name="one-liner-dark"
      args={{
        as: 'one-liner',
        color: 'dark',
        placeholder: 'Placeholder',
        disabled: false
      }}
    >
      {Template.bind({})}
    </Story>
</Canvas>

### Rounded Style

Use this style when you are using this component in a short form with enough space

<Canvas>
  <Story
    name="rounded-light"
    args={{
      as: 'rounded',
      color: 'light',
      placeholder: 'Placeholder',
      disabled: false
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="rounded-light-disabled"
    args={{
      as: 'rounded',
      color: 'light',
      placeholder: 'Placeholder',
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
    <Story
      name="rounded-dark"
      args={{
        as: 'rounded',
        color: 'dark',
        placeholder: 'Placeholder',
        disabled: false
      }}
    >
      {Template.bind({})}
    </Story>
</Canvas>

## Rendering an icon

Pass an icon as React component through the `icon` prop. It will render an icon on the right of the input.

<Canvas>
    <Story
      name="with icon"
      args={{
        icon: <AlertIcon size={12} />
      }}
    >
      {Template.bind({})}
    </Story>
</Canvas>
